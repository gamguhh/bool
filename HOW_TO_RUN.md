# 🎱 دليل تشغيل لعبة البلياردو المطورة

## 🚀 طريقة تشغيل اللعبة:

### **الطريقة الأولى - خادم محلي (الأفضل):**

```bash
# في Terminal/Command Prompt:
cd مجلد-اللعبة
python -m http.server 8000
```

ثم افتح المتصفح واذهب إلى: `http://localhost:8000`

### **الطريقة الثانية - Node.js:**

```bash
# إذا كان عندك Node.js:
npx http-server .
```

### **الطريقة الثالثة - PHP:**

```bash
# إذا كان عندك PHP:
php -S localhost:8000
```

### **الطريقة الرابعة - VS Code:**

1. افتح المجلد في VS Code
2. اضغط كليك يمين على `index.html`
3. اختر "Open with Live Server"

## 🎮 طريقة اللعب:

### **التحكم بالماوس:**
- حرك الماوس لتوجيه العصا
- اضغط واسحب بعيداً عن الكرة البيضاء لزيادة القوة
- اترك زر الماوس لإطلاق الضربة

### **التحكم بلوحة المفاتيح:**
- `W` - زيادة القوة
- `S` - تقليل القوة  
- `Esc` - العودة للقائمة

## ⚙️ الإعدادات:

يمكنك تعديل الإعدادات في `script/Global.js`:

```javascript
const AIM_LINE_ENABLED = true;      // خط التصويب
const POWER_BAR_ENABLED = true;     // شريط القوة
const VISUAL_EFFECTS_ENABLED = true; // التأثيرات البصرية
const BALL_TRAILS_ENABLED = true;   // مسارات الكرات
```

## 🔧 متطلبات التشغيل:

- متصفح حديث (Chrome, Firefox, Safari, Edge)
- خادم محلي (لا تفتح الملف مباشرة!)
- اتصال بالإنترنت ليس مطلوب بعد التحميل

## 🆘 حل المشاكل:

**إذا لم تعمل اللعبة:**
1. تأكد من تشغيلها عبر خادم محلي وليس بفتح الملف مباشرة
2. تأكد من وجود جميع الملفات في نفس المجلد
3. تحقق من وحدة التحكم في المتصفح (F12) لأي أخطاء

**إذا لم تظهر الصور:**
- تأكد من وجود مجلد `assets` مع جميع الصور

**إذا لم تعمل الأصوات:**
- تأكد من وجود مجلد `assets/sounds`
- تأكد من أن الصوت مفعل في المتصفح

## ✨ الميزات الجديدة:

- تحكم محسن بالماوس مثل 8 Ball Pool الأصلية
- خط تصويب ذكي مع انعكاسات
- شريط قوة تفاعلي مع ألوان
- تأثيرات بصرية للحركة والاصطدامات
- فيزياء واقعية للكرات

استمتع باللعب! 🎯